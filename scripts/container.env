# Environment variables passed into every hj::utility_run container invocation.

# Keeps the containerized AppHost's Next.js build output separate from a native
# `aspire start` run's.
NEXT_DIST_DIR=.next-container
